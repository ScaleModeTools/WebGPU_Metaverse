{
  "name": "@webgpu-metaverse/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "@fails-components/webtransport": "^1.6.1",
    "@fails-components/webtransport-transport-http3-quiche": "^1.6.1",
    "@webgpu-metaverse/shared": "0.1.0"
  },
  "scripts": {
    "clean": "node --input-type=module --eval \"import { rmSync } from 'node:fs'; rmSync('dist', { recursive: true, force: true });\"",
    "build": "npm run clean && tsc -p tsconfig.json",
    "watch:build": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "start": "node dist/index.js",
    "start:watch": "node --watch --watch-preserve-output --watch-path dist dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
